<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dieberater.list">
      <field name="name">dieberater list</field>
      <field name="model">dieberater.dieberater</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dieberater.action_window">
      <field name="name">dieberater window</field>
      <field name="res_model">dieberater.dieberater</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dieberater.action_server">
      <field name="name">dieberater server</field>
      <field name="model_id" ref="model_dieberater_dieberater"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dieberater" id="dieberater.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dieberater.menu_1" parent="dieberater.menu_root"/>
    <menuitem name="Menu 2" id="dieberater.menu_2" parent="dieberater.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dieberater.menu_1_list" parent="dieberater.menu_1"
              action="dieberater.action_window"/>
    <menuitem name="Server to list" id="dieberater" parent="dieberater.menu_2"
              action="dieberater.action_server"/>
    -->
  </data>
</openerp>